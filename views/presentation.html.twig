{% extends "base.html.twig" %}

{% block title %}Sport Event - Organisez vos événements sportifs{% endblock %}

{% block content %}
<div class="container-fluid px-0">
    <!-- Hero Section avec fond dégradé -->
    <div class="hero-section text-white py-5">
        <div class="container py-5">
            <div class="row align-items-center">
                <div class="col-lg-6 fade-in-left">
                    {% if session.user_id is not defined %}
                        <h1 class="display-4 fw-bold mb-4 text-shadow">Bienvenue sur Sport Event</h1>
                        <p class="lead mb-4 text-shadow-sm">Rejoignez notre communauté dynamique et participez à des événements sportifs passionnants près de chez vous.</p>
                        <div class="d-flex gap-3 button-group">
                            <a href="/sporteventultimate/inscription" class="btn btn-light btn-lg scale-hover">
                                <i class="fas fa-user-plus me-2"></i>S'inscrire gratuitement
                            </a>
                            <a href="/sporteventultimate/connexion" class="btn btn-outline-light btn-lg scale-hover">
                                <i class="fas fa-sign-in-alt me-2"></i>Se connecter
                            </a>
                        </div>
                    {% else %}
                        <h1 class="display-4 fw-bold mb-4 text-shadow">Bonjour !</h1>
                        <p class="lead mb-4 text-shadow-sm">Prêt pour de nouvelles aventures sportives ?</p>
                        <div class="d-flex gap-3 button-group">
                            <a href="/sporteventultimate/event/create" class="btn btn-light btn-lg scale-hover">
                                <i class="fas fa-calendar-plus me-2"></i>Créer un événement
                            </a>
                            <a href="/sporteventultimate/profile" class="btn btn-outline-light btn-lg scale-hover">
                                <i class="fas fa-user me-2"></i>Mon profil
                            </a>
                        </div>
                    {% endif %}
                </div>
                <div class="col-lg-6 text-center fade-in-right">
                    <div class="hero-icon-container">
                        <i class="fas fa-running fa-10x text-white-50"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {% if session.user_id is not defined %}
        <!-- Features Section pour visiteurs avec animation -->
        <div class="container py-5">
            <h2 class="text-center mb-5 section-title">Pourquoi nous rejoindre ?</h2>
            <div class="row g-4">
                <div class="col-md-4 fade-in" style="animation-delay: 0.2s">
                    <div class="feature-card card border-0 shadow-sm h-100">
                        <div class="card-body text-center p-4">
                            <div class="icon-circle bg-primary">
                                <i class="fas fa-calendar-plus fa-2x text-white"></i>
                            </div>
                            <h3 class="h5 fw-bold mt-4">Créez vos événements</h3>
                            <p class="text-muted mb-0">Organisez facilement des événements sportifs et gérez les inscriptions en quelques clics.</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 fade-in" style="animation-delay: 0.4s">
                    <div class="feature-card card border-0 shadow-sm h-100">
                        <div class="card-body text-center p-4">
                            <div class="icon-circle bg-success">
                                <i class="fas fa-users fa-2x text-white"></i>
                            </div>
                            <h3 class="h5 fw-bold mt-4">Rejoignez la communauté</h3>
                            <p class="text-muted mb-0">Participez à des événements, rencontrez d'autres sportifs et partagez votre passion.</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 fade-in" style="animation-delay: 0.6s">
                    <div class="feature-card card border-0 shadow-sm h-100">
                        <div class="card-body text-center p-4">
                            <div class="icon-circle bg-info">
                                <i class="fas fa-map-marker-alt fa-2x text-white"></i>
                            </div>
                            <h3 class="h5 fw-bold mt-4">Trouvez des événements</h3>
                            <p class="text-muted mb-0">Découvrez les événements sportifs près de chez vous et inscrivez-vous en un clic.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% else %}
        <!-- Section Actions Rapides pour utilisateurs connectés -->
        <div class="container py-5">
            <h2 class="text-center mb-5 section-title">Mes Actions Rapides</h2>
            <div class="row g-4">
                <div class="col-md-3 fade-in" style="animation-delay: 0.2s">
                    <a href="/sporteventultimate/event/create" class="text-decoration-none">
                        <div class="quick-action-card card border-0 shadow-sm h-100">
                            <div class="card-body text-center p-4">
                                <div class="icon-circle bg-primary">
                                    <i class="fas fa-calendar-plus fa-2x text-white"></i>
                                </div>
                                <h3 class="h5 fw-bold mt-4 text-dark">Créer un événement</h3>
                            </div>
                        </div>
                    </a>
                </div>
                <div class="col-md-3 fade-in" style="animation-delay: 0.4s">
                    <a href="/sporteventultimate/profile" class="text-decoration-none">
                        <div class="quick-action-card card border-0 shadow-sm h-100">
                            <div class="card-body text-center p-4">
                                <div class="icon-circle bg-success">
                                    <i class="fas fa-user fa-2x text-white"></i>
                                </div>
                                <h3 class="h5 fw-bold mt-4 text-dark">Mon profil</h3>
                            </div>
                        </div>
                    </a>
                </div>
                <div class="col-md-3 fade-in" style="animation-delay: 0.6s">
                    <a href="/sporteventultimate/home" class="text-decoration-none">
                        <div class="quick-action-card card border-0 shadow-sm h-100">
                            <div class="card-body text-center p-4">
                                <div class="icon-circle bg-info">
                                    <i class="fas fa-search fa-2x text-white"></i>
                                </div>
                                <h3 class="h5 fw-bold mt-4 text-dark">Explorer</h3>
                            </div>
                        </div>
                    </a>
                </div>
                <div class="col-md-3 fade-in" style="animation-delay: 0.8s">
                    <a href="/sporteventultimate/profile#my-events" class="text-decoration-none">
                        <div class="quick-action-card card border-0 shadow-sm h-100">
                            <div class="card-body text-center p-4">
                                <div class="icon-circle bg-warning">
                                    <i class="fas fa-list fa-2x text-white"></i>
                                </div>
                                <h3 class="h5 fw-bold mt-4 text-dark">Mes événements</h3>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    {% endif %}

    <!-- Upcoming Events Section avec animation -->
    <div class="container pb-5">
        <div class="row">
            <div class="col-12">
                <h2 class="text-center mb-5 section-title">
                    {% if session.user_id is not defined %}
                        Découvrez nos prochains événements
                    {% else %}
                        À ne pas manquer
                    {% endif %}
                </h2>
            </div>
        </div>
        {% if upcomingEvents|length > 0 %}
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
                {% for event in upcomingEvents %}
                    <div class="col fade-in" style="animation-delay: {{ loop.index * 0.2 }}s">
                        <div class="event-card card border-0 shadow-sm h-100">
                            <div class="card-body p-4">
                                <h5 class="card-title fw-bold">{{ event.title }}</h5>
                                <p class="card-text text-muted">{{ event.description|slice(0, 100) }}...</p>
                                <div class="event-details mb-3">
                                    <div class="d-flex align-items-center mb-2">
                                        <i class="fas fa-calendar me-2 text-primary"></i>
                                        <span>{{ event.date_event|date("d/m/Y") }}</span>
                                    </div>
                                    <div class="d-flex align-items-center mb-2">
                                        <i class="fas fa-clock me-2 text-success"></i>
                                        <span>{{ event.time_event|date("H:i") }}</span>
                                    </div>
                                    <div class="d-flex align-items-center">
                                        <i class="fas fa-map-marker-alt me-2 text-danger"></i>
                                        <span>{{ event.location }}</span>
                                    </div>
                                </div>
                                <div class="text-end">
                                    {% if session.user_id is not defined %}
                                        <a href="/sporteventultimate/connexion" class="btn btn-primary scale-hover">
                                            <i class="fas fa-sign-in-alt me-1"></i>Participer
                                        </a>
                                    {% else %}
                                        <a href="/sporteventultimate/event/{{ event.id }}" class="btn btn-outline-primary scale-hover">
                                            <i class="fas fa-eye me-1"></i>Voir
                                        </a>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <div class="alert alert-info text-center fade-in">
                <i class="fas fa-info-circle me-2"></i>
                Aucun événement à venir pour le moment.
                {% if session.user_id is defined %}
                    <a href="/sporteventultimate/event/create" class="alert-link">Créez le premier événement !</a>
                {% endif %}
            </div>
        {% endif %}
    </div>

    <!-- Call to Action avec animation -->
    {% if session.user_id is not defined %}
        <div class="cta-section text-white py-5">
            <div class="container text-center py-4 fade-in">
                <h2 class="mb-4 text-shadow">Prêt à rejoindre la communauté ?</h2>
                <div class="d-flex justify-content-center gap-3">
                    <a href="/sporteventultimate/inscription" class="btn btn-light btn-lg scale-hover">
                        <i class="fas fa-user-plus me-2"></i>S'inscrire maintenant
                    </a>
                    <a href="/sporteventultimate/connexion" class="btn btn-outline-light btn-lg scale-hover">
                        <i class="fas fa-sign-in-alt me-2"></i>Se connecter
                    </a>
                </div>
            </div>
        </div>
    {% endif %}
</div>

<style>
/* Animations */
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}

@keyframes fadeInLeft {
    from { opacity: 0; transform: translateX(-50px); }
    to { opacity: 1; transform: translateX(0); }
}

@keyframes fadeInRight {
    from { opacity: 0; transform: translateX(50px); }
    to { opacity: 1; transform: translateX(0); }
}

.fade-in {
    opacity: 0;
    animation: fadeIn 0.8s ease-out forwards;
}

.fade-in-left {
    opacity: 0;
    animation: fadeInLeft 0.8s ease-out forwards;
}

.fade-in-right {
    opacity: 0;
    animation: fadeInRight 0.8s ease-out forwards;
}

/* Sections stylisées */
.hero-section {
    background: linear-gradient(135deg, #0d6efd 0%, #0dcaf0 100%);
    position: relative;
    overflow: hidden;
}

.hero-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 320"><path fill="rgba(255,255,255,0.1)" fill-opacity="1" d="M0,96L48,112C96,128,192,160,288,160C384,160,480,128,576,112C672,96,768,96,864,112C960,128,1056,160,1152,160C1248,160,1344,128,1392,112L1440,96L1440,320L1392,320C1344,320,1248,320,1152,320C1056,320,960,320,864,320C768,320,672,320,576,320C480,320,384,320,288,320C192,320,96,320,48,320L0,320Z"></path></svg>');
    background-position: center bottom;
    background-repeat: no-repeat;
    opacity: 0.1;
}

.cta-section {
    background: linear-gradient(135deg, #6f42c1 0%, #0d6efd 100%);
    position: relative;
    overflow: hidden;
}

/* Éléments stylisés */
.text-shadow {
    text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
}

.text-shadow-sm {
    text-shadow: 1px 1px 2px rgba(0,0,0,0.1);
}

.section-title {
    position: relative;
    padding-bottom: 15px;
}

.section-title::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 50px;
    height: 3px;
    background: linear-gradient(to right, #0d6efd, #0dcaf0);
    border-radius: 3px;
}

.icon-circle {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto;
    transition: transform 0.3s ease;
}

/* Cartes et boutons */
.feature-card, .quick-action-card, .event-card {
    transition: all 0.3s ease;
    border-radius: 15px;
    overflow: hidden;
}

.feature-card:hover, .quick-action-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 1rem 3rem rgba(0,0,0,.175)!important;
}

.event-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 .5rem 1rem rgba(0,0,0,.15)!important;
}

.scale-hover {
    transition: transform 0.2s ease;
}

.scale-hover:hover {
    transform: scale(1.05);
}

.button-group .btn {
    border-radius: 10px;
    transition: all 0.3s ease;
}

.event-details {
    font-size: 0.9rem;
}

/* Responsive */
@media (max-width: 768px) {
    .hero-section {
        text-align: center;
    }
    
    .button-group {
        justify-content: center;
    }
    
    .hero-icon-container {
        margin-top: 2rem;
    }
}
</style>
{% endblock %}